# Backlog

Status labels: **completed** (merged, tested, verified against independent data where
that applies), **partial** (merged but with a documented gap), **unstarted**.
This file is the single list; the completion report links here. The expansion
programme's consolidated table further down uses the same three ideas under the names
**completed** / **partial** / **open** (open = unstarted, or a design question still
waiting on a decision).

## Required first release (from the brief)

| item | status | notes |
|---|---|---|
| Observation input: versioned JSON, CSV, validation | completed | `skyfix.session/1`; CSV round-trips; rejects invalid angles, non-finite values, bad timestamps, unknown bodies without a supplied direction |
| First numerical slice (supplied GHA/Dec, corrected altitude) | completed | `reference-philadelphia-5star-geocentric` recovers truth to 0.000 m |
| Sight reduction with per-step corrections | completed | six steps always reported; artificial-horizon halving tested; low-altitude refraction flagged/rejected |
| Position solver (weighted NLLS, multistart, ambiguity, conditioning) | completed | one sight -> circle; two sights -> both intersections; disjoint circles -> no fake point |
| Quality output (a-priori covariance, nominal 95 % ellipse, suppression rules) | completed | Monte Carlo: 95.1 % coverage under independent noise; 42 % under a shared bias |
| Corrections: stars + Sun path, horizon modes, refraction validity | completed | Sun SD/HP from the ephemeris; the Moon (augmented SD, rigorous parallax) and the four navigational planets (Venus at its centre of light) followed in the explorer redesign — see "Explorer redesign" below |
| Offline astronomy: Sun + stars, fixture-pack fallback | completed | stars 0.0011', Sun 0.0026' vs Skyfield; pack says "limited-date operation" |
| Interface: CLI + browser workbench, bundled assets | completed | CLI has grown to 25 subcommands with the explorer redesign (`skyfix --help`: the original `validate reduce solve catalog coverage convert demos simulate experiment plan`, plus `sky events phases seasons noon polaris average running-fix predict lunar plan-sights almanac eclipses eclipse planet-events`); every export backed by real code, no feature gates, no stubs; the browser ships the explorer, bundled with no CDN fonts, scripts or map tiles, at `/`, the site's home page since the switch-over on 2026-09-24 (`/next/`, its address while it was built, forwards there); the original workbench, kept for reference at `/classic/` after the switch-over, was retired in the expansion programme and its address now forwards to the explorer's views (`docs/EXPLORER_GUIDE.md`) |
| Simulator: seeded, truth separate, six error scenarios | completed | ten scenarios; experiment summaries in CSV/JSON |
| Observation planner | completed | geometry-driven greedy ranking with disclosures |

## Follow-up modules

| item | status | notes |
|---|---|---|
| A. Stationary camera sextant (synthetic images) | partial | attitude and position kept apart by type; full render-to-fix chain proven end to end, 69 tests (`cargo test -p skyfix-camera`) — but only on synthetic images. The module's own gap: the 58-star closed-world catalogue identifies on an 80-degree lens but fails a plausible 40-degree one at the briefed pointing (CAMERA.md section 4), and "nothing in this crate has met a real lens, a real sensor or a real sky" (CAMERA.md section 9) |
| B. Polarization compass laboratory | completed | 180-degree ambiguity always returned, never guessed; 76 tests (`cargo test -p skyfix-polar`). Scoped to heading estimation from a stated sky model, as designed — see "Polarization geolocation" below, which is the deliberately deferred remainder |
| Navigation methods: noon sight, latitude by Polaris, averaging a run, running fix, and (wave 2) Moon and planet sights and lunar distance | completed | `crates/skyfix-core/src/methods/` and `sights::lunar`, WASM exports in `crates/skyfix-wasm/src/{nav,navsky}.rs`; validated against Skyfield truth, Bowditch's worked examples and seeded coverage (`docs/NAVIGATION_METHODS.md` section 6, `docs/NAVIGATION_SKY.md`). Reachable two ways: the explorer's **Navigate** view (every method, live corrections, GPX/CSV/JSON, browser-local autosave) and the `skyfix` CLI (`noon polaris average running-fix lunar plan-sights`). The documented gap: the Polaris sigma under-covers (89.8 %) within 1.5° of the pole when the DR's longitude is uncertain by tens of degrees |
| C. Motion and independent navigation checks | partial | disagreement statements, never a diagnosis; 58 tests (`cargo test -p skyfix-motion`). The module's own gap: a running fix's nominal 95 % ellipse is a documented lower bound, measured at 92.3 % coverage over 300 seeded repetitions, because the dead-reckoning error behind every sight in one fix shares the same speed and course bias and the solver is never told so (MOTION.md section 3) |

## Explorer redesign

| item | status | notes |
|---|---|---|
| Moon and planets (ephemeris) | completed | `skyfix_ephemeris::moon` (ELP 2000-82B) and `::planets` (VSOP87A), apparent geocentric of date, 1990-2060; every navigational body (Sun, Moon, Venus, Mars, Jupiter, Saturn, 58 stars) validated against Skyfield/JPL DE440s well inside the 0.1′ target — see `docs/ACCURACY.md`, "At a glance" and sections 2 ("Planets") and 7 |
| Deeper star catalogue, for display | completed | `skyfix-starfield`: 9,095 stars (NASA HEASARC's BSC5P, no credit required), 88 constellation figures drawn for this project, IAU boundaries; feeds the explorer's Sky view and map ground points only — kept out of `skyfix-core`/`skyfix-ephemeris`/`skyfix-sim`/`skyfix-almanac` by a crate-boundary test, so it can never reach a fix. Not the same catalogue as the camera module's (below) |
| Moving observer, reached through the running fix | completed | `skyfix-motion::running_fix` (advances each sight along the dead-reckoning track, inflates its sigma) is now reachable end to end: the explorer's Navigate view, the WASM export, and `skyfix running-fix`. Its own documented gap is unchanged — see Module C above, and "First-class moving observer inside the solver" below for the different, larger project this does not replace |
| Explorer browser UI: Map/Globe, Sky, Charts, Almanac, Navigate, Learn, About | completed | see `docs/EXPLORER_GUIDE.md`; the site's home page since the switch-over on 2026-09-24; offline after the first visit and installable (service worker, manifest, icons: `web/README.md`, "Offline and installable") |
| Explorer browser UI: Events (eclipses, Moon phases, equinoxes, solstices, planet events) | completed | the Events view (eclipse cards with local circumstances and map paths, Moon phases, seasons, oppositions, conjunctions, elongations, closest approaches); engine validated against NASA's canon and SKYCAL and Skyfield (`docs/ACCURACY.md` sections 12 and 13); CLI `skyfix eclipses`, `eclipse <id>`, `planet-events` |

## Unstarted, with the reasoning

| item | status | notes |
|---|---|---|
| Retire the original workbench at `/classic/` | completed | Retired in the expansion programme (2026-09-24, packs agent): its page code (`web/src/main.ts` and the modules only it used, `web/src/pwa/workbench-prompt.ts`) is deleted and nothing precaches it. `/classic/` now forwards to the home page, keeping the query and mapping the old views' fragments (`#observations`, `#corrections`, `#fix`, `#planner` → `#navigate`, `#simulator` → `#learn`, `#about` → `#about`): a static page for a first visit, and the service worker's own forwarding page offline (`REDIRECT_PAGES` in `web/vite.config.ts`, `forwardPageHtml` in `web/src/sw/policy.ts`); `web/scripts/offline-check.mjs` checks both. Navigate and Learn carry everything it did |
| Deeper star catalogue for the camera module | unstarted | Real cameras see thousands of stars; the 58-star closed world works only in simulation. The explorer redesign has since built exactly the kind of catalogue this needed for a different purpose — `skyfix-starfield`'s BSC5P table (9,095 stars, permissively licensed, no credit required) — but only for display: it is kept out of every navigation and camera crate by design (crate-boundary test), and the camera module's own closed-world identification still uses the original 58 stars. Reusing BSC5P's data for the camera's pair-angle search is now a much smaller task than sourcing a new catalogue, but the search structure (k-vector or geometric hashing) and the camera-specific wiring are still unstarted. |
| First-class moving observer inside the solver | unstarted | The running fix (Module C, completed, and now reachable through the explorer's Navigate view and the CLI — see "Explorer redesign" above) advances each sight's position along the dead-reckoning track and inflates its sigma, which treats the DR error as independent per sight. A first-class model would instead treat the DR error as *correlated* across every sight in one fix (the same speed-and-course bias sits behind all of them), which needs it built into the solver's Jacobian rather than added to each sigma afterwards. This is a different, larger project than the running fix, not a subset of it. |
| Real sextant sights | unstarted | No hardware yet; the artificial-horizon path is implemented and tested on synthetic logs. |
| Camera on real imagery | unstarted | Lens calibration, a real local vertical, timing, rolling shutter. |
| Polarization geolocation | unstarted | Deferred by design; heading first. |
| Satellites | considered, not planned | Raised and set aside, not merely not yet started. SkyFix Lab's navigational bodies (CONVENTIONS section 10, 13.1) are the Sun, Moon, planets and fixed stars: bodies an offline analytic or semi-analytic theory predicts accurately for decades, matching the project's offline-first design. A satellite's position instead needs orbital elements (a TLE or similar) that go stale within days to weeks, which would either need a network fetch (breaking "no network, ever") or a bundled set that silently ages out of date — a different kind of staleness than anything else in this project, and a different kind of navigation than sextant celestial navigation. |
| The lunar limb profile in eclipse contacts | completed | Stale by 2026-09-25 (docs3): this was written before the expansion programme's P12 (`eclipselimb` agent) built exactly this, from LRO LOLA LDEM_16 topography, as the optional `lunar-limb` pack — see "The lunar limb (P12)" in the table below. Eclipses without the pack still use the mean limb, labelled as such. |

## Expansion programme

One table for every agent's own backlog rows (formerly ~21 separate tables under one
heading per agent) and the verifier's own findings that are not one engine's row,
consolidated by the documentation pass (docs3, 2026-09-25): same rows, the same wording
wherever a status did not simply need renaming to `completed` / `partial` / `open`, sorted
by status so what is left stands out. Three rows the agents themselves flagged as closed
by a later one are folded into that later row instead of kept as two: the almanac's stale
daily-pages note (closed by cli3's row of the same name), photo's "Aiming the Sky view"
and Tonight's "Show in Sky" centring (each closed by sky2's row). `partial` keeps the
old `known` / `known limit` / `available, not default` rows, where something is merged
and working with one documented gap; `open` keeps `unstarted` and design questions still
waiting on a decision; `completed` keeps a recorded decision (`decided against`, `by
decision`) as well as finished work, since nothing is left pending either way.

| area | item | status | notes |
|---|---|---|---|
| Sun tools | Sun tools engine: golden/blue hour, azimuth search, alignments, analemma, sun path, rise/set azimuths, equation of time, clear-sky energy, Milky Way windows | completed | (engine, WASM, TypeScript, mock). `skyfix_almanac::sun_tools`; validated in `docs/ACCURACY.md` section 14. The interface is wave 2 (`photo`, `charts2`); the CLI is `cli3` |
| Sun tools | Forward the sun tools through the memoising engine | completed | (planner, 2026-09-25). `memoEngine` in `web/src/next/component.ts` now passes every other capability of the engine through, bound to the engine, so the type guards of every expansion engine see it on the wrapper; the ten named methods keep their memo |
| Sun tools | `events::roots` visible to the crate | completed | `sun_tools` compiles the event finder's Brent routines from the same file (`#[path]`, with a lint allowance) because `events::roots` is private; making it `pub(crate)` lets `sun_tools` use it directly **polish2:** `events::roots` is `pub(crate)` and `sun_tools` uses it (`pub(crate) use crate::events::roots`); the `#[path]` include and its lint allowance are gone (polish list 4) |
| Moonshape (P1) | The Earth's shape in Moon sight reduction | completed | Moved here from "Unstarted". The Moon's model altitude carries the exact WGS84 Earth-shape term (up to 0.24′) wherever a model altitude is evaluated — the solver at every trial position, the intercept at the assumed position, predicted readings, the noon and averaging methods, the planner, the misfit grid, the running fix and the Moon's circle of position — while the correction chain and `Ho` stay on the sphere (CONVENTIONS 15.4, `skyfix_core::sights::wgs84::EarthShape`). WGS84 Moon sights reduce to within 0.0064′ of the model; the WGS84 Moon sessions fix 2.6 m and 5.1 m from the truth (17.9 m and 34.9 m before); the Moon's parallax through the model agrees with USNO's to 0.0004′ (`docs/ACCURACY.md` section 10). Not applied to the planets (at most 0.0021′) |
| Moonshape (P1) | Seconds omitted from a sight time | completed | The Navigate sight form takes `HH:MM` as `:00` and says "Seconds omitted: :00 assumed; each second is 0.25′ of longitude" (`web/src/next/navigate/parse.ts`) |
| Moonshape (P1) | Error-budget rows for the dip anomaly and anomalous refraction | completed | `docs/ACCURACY.md` section 4, each pointing to the shared-bias estimate (`SolveOptions.estimate_shared_bias`, `skyfix solve --bias`) as the remedy for the part the sights share |
| Magnetic variation and compass error (geomag) | Magnetic variation anywhere, 1900-2030 (WMM2025, IGRF-14), with the model's uncertainty, zones and a grid for isogonic lines | completed | (engine). `crates/skyfix-geomag`, exports `magnetic_field`, `magnetic_grid` (`crates/skyfix-wasm/src/geomag.rs`); validated against NCEI's, IAGA's, BGS's and NOAA's values (`docs/ACCURACY.md` section 15). The interface (Selected card, Navigate → Compass, map layer) is wave 2 |
| Magnetic variation and compass error (geomag) | Compass error by azimuth and by amplitude, with variation and deviation | completed | (engine). `skyfix_core::methods::compass`, export `compass_error`; Bowditch ch. 15 reproduced (`docs/NAVIGATION_METHODS.md` section 9). CLI `skyfix variation` / `skyfix compass-error` left to the `cli3` agent |
| Time scales (timescales) | ΔT model with uncertainty, UTC/UT clock, IERS DUT1 history, Julian/Gregorian calendars, expanded years, `time_info`, `set_dut1`, `calendar_convert`, `skyfix calendar`, saros for any epoch | completed | CONVENTIONS 15.2-15.3; `docs/ACCURACY.md` section 17; `docs/EXPLORER_API.md`, "Time scales, Delta-T and calendars" |
| Time scales (timescales) | Refresh the IERS UT1 − UTC table from a current `finals2000A.all` | completed | 2026-09-25: observed to 2026-09-24, IERS Bulletin A's prediction to 2027-09-28; the columns used are committed as `tools/timescales/sources/finals2000A-ut1-2026-09-25.txt`. Refresh again (download, `gen_timescales.py`, review) before 2027-09-28, when DUT1 becomes unknown (0 ± 0.9 s) again |
| Sailings (wave 1) | Sailings: great-circle, rhumb-line (sphere or WGS84 meridional parts), mid-latitude, plane, traverse, parallel and composite sailing; waypoints; ETA | completed | (engine). `skyfix_core::sailings`, WASM `sailing`; every Bowditch 2019 ch. 12 worked example reproduces (`docs/NAVIGATION_METHODS.md` section 9.9); the Navigate → Passage tab and the map drawing are wave 2 (navigate2) |
| Sailings (wave 1) | Forward dead reckoning and routes (legs in the running fix's shape) | completed | (engine). `dr_advance`, `route_positions`; a great-circle route equals the running fix's DR to 1 mm; feeding a drawn DR track into the running fix is navigate2's |
| Sailings (wave 1) | Dip short of the horizon (`shore` horizon) | completed | (engine). Bowditch Table 14 to 0.046′; the TS types, CSV, autosave, the classic and Navigate horizon selects keep a shore horizon, but no form can yet *set* its distance (navigate2: a distance field beside the horizon select) |
| Sailings (wave 1) | Star identification from altitude and bearing | completed | (engine). `star_identify`; 58/58 stars recovered; the UI is navigate2's. Variation is not looked up: it is the caller's (the geomag agent's WMM value can be passed as `variation_deg`) |
| Sailings (wave 1) | Star finder (2102-D equivalent) geometry | completed | (engine). `star_finder_geometry`; drawing it is almanac2's or navigate2's |
| Sailings (wave 1) | Index-error and watch logs | completed | (engine). session schema, interpolation, the reduced sight's record; the Navigate view's `instrumentJson` (web/src/next/engine/wasm-nav.ts) still sends only the single index correction to `predict_sextant` and `plan_sights`, so a UI with a log must pass the log too (navigate2); no form edits the logs yet |
| The Moon in detail (P8) | Libration, axis position angle, terminator, sub-solar point, disc geometry | completed | (engine). `skyfix_almanac::libration`, WASM `moon_orientation`; within 0.006° of JPL's DE440 lunar orientation 1550–2650 (`docs/ACCURACY.md` section 18). The Sky inset that draws it is wave 2 (Q3) |
| The Moon in detail (P8) | Named features on the terminator | completed | (engine). 150 features from the USGS/IAU gazetteer (public domain), `moon_features`; the list and the inset are wave 2 (Q3, Q2) |
| The Moon in detail (P8) | Perigee, apogee, supermoons, the year's largest and smallest full Moon | completed | (engine). `moon_apsides`, within 11 s and 0.22 km of DE440s; the Events list is wave 2 (Q4) |
| The Moon in detail (P8) | Lunar occultations of bright stars and planets with local times | completed | (engine), mean limb. `occultations`, within 1.4 s of Skyfield's geometry; the Events list and Selected card are wave 2 (Q4). Contacts are for the mean limb, labelled; correcting them (and deciding grazes) with the real limb profile waits on the lunar-limb pack (P12) |
| Deep sky (deepsky) | Deep-sky objects (110 Messier + 103 by a stated rule), meteor showers (32, dates from our Sun), Milky Way outline (COBE/DIRBE isophotes), IAU WGSN star names (+220), search, extinction and limiting magnitude, the "tonight" ranking | completed | (engine). `skyfix-starfield` (`dso`, `showers`, `milkyway`, `names`, `search`, `extinction`, `tonight`, `observe`), exports in `crates/skyfix-wasm/src/deepsky.rs`, `DeepSkyEngine` in types.ts with the mock; display-only (CONVENTIONS 13.6); checks in `docs/ACCURACY.md`, "Deep sky". The views that show them belong to the Sky and Tonight packages |
| Tides | Tide predictions engine and the `tides-us` pack | completed | (engine). `skyfix-tides` + `skyfix-wasm::tides`: NOAA's 3 499 stations, harmonic prediction with Schureman node factors in NOAA's conventions, high and low water with NOAA's tide-table rule, subordinate stations, datums, nearest stations; within 1.36 min and 1.08 cm of NOAA's own predictions at every station tested (`docs/ACCURACY.md` section 20). The interface (Charts → Tides, map layer, Tonight line) is wave 2 |
| Tides | Tide pack loading through the pack mechanism | completed | `tides-us` is an entry of `packs::PRODUCERS`; `load_pack("tides-us", bytes)` installs it (the temporary loader of the first draft is gone) |
| Planet detail (planetdetail) | Galilean moons, Saturn's rings, planet discs, transits of Mercury and Venus with local circumstances, conjunctions and stations, the Earth's apsides, comets and asteroids from supplied elements | completed | (engine). `skyfix_almanac::{satellites, rings, discs, transits, conjunctions, earth_apsides, orbits}`, exports in `crates/skyfix-wasm/src/planetdetail.rs`, `PlanetDetailEngine` in types.ts with the mock; `docs/ACCURACY.md` section 21. The eyepiece insets are wave 2 (Q3), the lists wave 2 (Q4) |
| Planet detail (planetdetail) | Core-module size | completed | The package adds 191 KB raw and 77 KB gzipped on main 3f4fe4e, taking the module to 2 997 407 / 1 231 646 bytes, 2.6 KB under the 3 MB raw budget (mostly code: E5, the searches, the MPC parser, the serialisation of eleven calls). `opt-level = "z"` (the polish agent's lever) or a lazily loaded second module would restore headroom **polish2:** with the deep-time series and built at `opt-level = "z"` the module is 2 549 965 bytes raw, 1 152 112 gzipped (ACCURACY section 32) |
| Charts (Q5) | Sun tab: sun path (from above, along the horizon), analemma, sunrise and sunset bearings, equation of time and declination, solar panel (clear-sky estimate) | completed | `web/src/next/charts/{sun-path,analemma,sun-bearings,eot,solar}.ts` over the suntools engine; `EXPLORER_GUIDE.md` "Charts" |
| Charts (Q5) | Tides tab: nearest stations, day or week curve with the app's time, high and low water, datums, the pack's Get state, "Show on the map" | completed | `charts/tides.ts`; the cursor's readout is read off the curve (`docs/ACCURACY.md`, "Charts") |
| Charts (Q5) | Moon tab: the Moon at one hour through the year; perigee, apogee, supermoons and micromoons on the month grid | completed | `charts/moon-year.ts`, `moon-calendar.ts` |
| Charts (Q5) | Save menu on every chart: PNG with a caption strip (light colours), CSV of the Table view, print one chart per page, Web Share | completed | `web/src/next/export/{png,csv}.ts` (shared), `charts/export-menu.ts`, `charts/print.ts` |
| Deep time in the interface (Q1) | Calendars, years, UT/UTC, LMT, tiers and the ±ΔT chip in the time bar, Settings and About | completed | `web/src/next/time/` (helpers every view uses: CONVENTIONS 15.6), `timebar/`, `playback.ts`; checks in `web/scripts/ui-check.mjs` (group `time`) |
| Deep time in the interface (Q1) | Views that still write a literal "UTC" beside times that may be UT | completed | `events/`, `almanac/`, `navigate/`, `panel/selected.ts`, `panel/place.ts` (`formatOffset(…)` without the instant: "UTC−5:00:40" in 585 BC): use `scaleLabel(jd)` and `formatOffset(ms, jd)` **polish2:** the rest (the charts' captions and tables, Navigate's time fields and tables, Learn's simulator) name the clock's scale; `web/test/next/polish-strings.test.ts` fails on a "(UTC)", "in UTC" or "hover for UTC" in any view's source |
| Deep time in the interface (Q1) | The panel's zone reason before 1850 | completed | `panel/place.ts` says "From the place: America/New_York…" while the clock is local mean time; `lmtReason` and `zoneTooltip` give the words **polish2:** checked: before 1850 the panel gives `lmtReason` (navigate2) |
| Deep time in the interface (Q1) | "1990–2060" and "1990 to 2060" in the interface | completed | `navigate/session-panel.ts:140`, `events/lists.ts:137, 263, 424, 425`, `events/eclipses.ts:749, 758`, `almanac/almanac.ts:557` (the date input's range), `panel/when.ts:82`; `tierNotice`, `sightsOnlyText` and `outsideCoverage` name the real bounds **polish2:** the session panel's help names the validated years from the engine; the Events lists say their own engine's span (`listCoverage`); the source scan fails on a literal 1990 or 2060 |
| Deep time in the interface (Q1) | Navigate's sight-time field and expanded years | completed | `navigate/parse.ts` `UTC_PATTERN` takes four-digit years only; a `-0584-…` time gets a format error instead of "Sights are offered only between 1550 and 2650" **polish2:** checked: `UTC_PATTERN` takes signed years (`navigate-sightextras.test.ts`), and a time in 585 BC gets "Sights are offered only between 1550 and 2650" |
| Deep time in the interface (Q1) | The CLI's help text | completed | `crates/skyfix-cli/src/cli.rs:193` says "from 1990-01-01 to 2060-12-31" **polish2:** checked: the help takes the span from the engine (`validated_span()`, cli3) |
| Deep time in the interface (Q1) | Phone Map outside the coverage: notices over the map's controls | completed | On a phone, a date outside the coverage shows two notices (the coverage one, and the Map's own "positions could not be computed" error), which cover the map's floating Layers button (ui-check's layout check, any date outside the years, e.g. 2080: it predates the deep-time work). The Map should not raise an error where `covered()`/`tierAt` already say nothing is computed, and the notice bar could leave the floating controls clear **polish2:** the map raises no error where nothing is computed; the notices take their own strip above the view (`--stage-notice-inset`), folded to one line on a phone |
| Deep time in the interface (Q1) | Views with their own per-day work during fast playback | completed | above 8 days a second `sunToday`/`aroundToday` pause; the Map (`seasons` per year, `sampleBodies` per day) and panel Place (`guessZone` each hour) views used to keep working per frame; `fastPlayback(state)` is the switch. **polish2:** the Map view draws its world layers four times a second and no per-day path while `fastPlayback`; the Place section stops following the hour. The Sky view's own per-day work is closed separately, by sky2 (see "Sky view astronomy layers"). |
| Selected card's tools (Q8, photo) | Golden and blue hour, the alignment finder with the map picker, "When is it at…?" by bearing, the Milky Way planner, the Moon in detail, RA/Dec and the magnetic bearing on every body, the predicted sextant reading, the tides line in Place | completed | `web/src/next/panel/{photo,moon-tools,alignment,when,selected}.ts`, `web/src/next/map/pick.ts`; `docs/ACCURACY.md` section 24 |
| Selected card's tools (Q8, photo) | The planets' apparent size | completed | planetdetail's `planet_disc` (the equatorial diameter, and the polar one in the tooltip), a quarter-hour at a time |
| Selected card's tools (Q8, photo) | The ±ΔT chip and the tier helper | completed | time-ui's `uncertaintyText` after every time (`deltaTNote`), the chip beside each tool's heading, `sightsOffered` / `sightsOnlyText` for the predicted reading, `scaleLabel` on the cards' second clock |
| Selected card's tools (Q8, photo) | Opening Charts on its Tides tab | completed | "Tides chart" and "The Sun's bearings through the year" call charts2's `showCharts` through a dynamic import (the Charts module stays lazily loaded) |
| Selected card's tools (Q8, photo) | time-ui's rows on a literal "UTC" and on "1990–2060" | completed | for the photo agent's files. `panel/selected.ts` writes the second clock with `scaleLabel`; `panel/when.ts`, the finder, the planner and golden hour name the real bounds (`outsideWords`, from `coverageBounds`). `panel/place.ts` (its `formatOffset` without the instant) is navigate2's |
| Selected card's tools (Q8, photo) | Offering the tides pack from the Place section | completed | (decided against — see notes). the line appears only when the pack is on the device; offering it everywhere would put a US-only download in front of every visitor, and knowing that a place is near a US station needs the pack itself |
| The lunar limb (P12) | The `lunar-limb` pack and limb-corrected solar-eclipse contacts, the drawn limb, approximate Baily's beads | completed | (engine). `skyfix_almanac::eclipses::limb`, WASM `eclipse_local_limb`, `lunar_limb_profile`, `lunar_limb_info`, the `lunar-limb` producer; TypeScript `eclipseLocal(id, observer, { limb: true })` and `LimbEngine`; `docs/ACCURACY.md` section 25. Showing the corrected times, the limb drawing with the Sun's disc and the beads, and the pack prompt in the eclipse card is wave 2 (Q4 events2) |
| The lunar limb (P12) | The core module's size budget | completed | The lunar limb adds 45.3 KB raw / 20.7 KB gzipped to the core module; main alone (ab8f55c) is 2 997 407 bytes, 2.6 KB under the 3 MB budget, so the module is 42.7 KB over it (and 2.3 KB over 1.25 MB gzipped). Options: raise the budget, `opt-level = "z"` (about 5 %), or load the limb engine as a second module with its pack **polish2:** the budget was raised to 3 MB / 1.25 MB and the module is built at `opt-level = "z"`: 2 549 965 bytes raw, 1 152 112 gzipped (ACCURACY section 32) |
| Sky view astronomy layers (Q3, sky2) | Search, deep-sky objects, the Milky Way, the RA/Dec grid, fields of view, the sky's darkness and extinction, meteor radiants, the "Up close" insets, added comets and asteroids, tonight's sights ringed, save as a picture | completed | `web/src/next/sky/**`; the panel search's "Sky objects" group (`panel/search.ts`); `docs/ACCURACY.md` section 26; ui-check group `sky2` |
| Sky view astronomy layers (Q3, sky2) | Aiming the Sky view from the Selected card (photo's two `// sky2:` hooks) | completed | "Show in Sky" marks the engine's galactic centre (`showInSky` with a `point`) at the best moment; "See it up close" opens the Moon's close-up with the card's features ringed (`openUpClose(ctx, 'Moon', { features })`). The photo section's row "Aiming the Sky view" above can be closed |
| Sky view astronomy layers (Q3, sky2) | Dome zoom (pinch and wheel on the dome) | completed | wheel, pinch, + and −, drag to pan, 0 or Whole sky to return; up to 12×; the labels adapt (names and deep-sky objects fainter as the chart grows) |
| Sky view astronomy layers (Q3, sky2) | The Sky view in fast playback | completed | faster than 8 days a second (`fastPlayback`) the view keeps its star places and deep-sky objects, pauses the selected body's path, the showers and tonight's estimates, and remakes the Milky Way at most ten times a second; all of it is redrawn when time slows. The Sky part of "Views with their own per-day work during fast playback" above can be closed |
| Sky view astronomy layers (Q3, sky2) | "Show in Sky" and "See it up close" from Tonight (the `skyTargets` channel) | completed | the channel moved to `sky/sky-link.ts` (Tonight imports it from there) and gained `take()`; the Sky view takes a target once the time has stopped gliding, centres the dome on it at 3× if it showed the whole sky (`SHOW_ZOOM`; the Selected card's and the panel search's requests do the same), opens its card, and opens the Moon's, Jupiter's or Saturn's close-up for `inset`; a direction (the Milky Way's core) is turned back into a J2000 place at that moment (`sky/targets.ts`). The Tonight section's row "Show in Sky centring the Sky view…" can be closed |
| Sky view astronomy layers (Q3, sky2) | One "how dark is your sky" for Tonight and the Sky view | completed | the Sky view's is a stored setting (`settings.skyQuality`, `skyBortle`, `skyNelm` in `state.ts`); Tonight's "Your sky" Bortle selector is kept per page. Tonight could read and write `settings.skyBortle` so the two agree **polish2:** one stored setting for Settings → Sky, the Sky view's Layers, Tonight's "Your sky" and the Events meteor rates (`sky/sky-choice.ts`) |
| Almanac tables and three-day pages (Q7, almanac2) | Three-date openings as the printed almanac lays them out | completed | `skyfix_almanac::opening`, `almanac_opening`; the view's **Three dates** mode (the default); grouped from 1 January in the display calendar, across the 1582 reform; A4 and US Letter, one sheet a page (`ui-check.mjs`) |
| Almanac tables and three-day pages (Q7, almanac2) | Increments and Corrections, Altitude Correction Tables (Sun, stars and planets, dip, the Moon's two parts, non-standard conditions, Venus and Mars), Polaris, Arc to Time | completed | `skyfix_almanac::tables`, six exports in `crates/skyfix-wasm/src/almanac_tables.rs`; each printable with a how-to sentence and a worked example; `docs/ACCURACY.md`, "Almanac tables and three-day pages" |
| Almanac tables and three-day pages (Q7, almanac2) | Any year in the Almanac | completed | the date entry reads years as the time bar does (`parseYear`), Julian before 1582-10-15 through the shared calendar; the ± chip on labelled-tier pages; the anachronism note before 1767; `pages::UtDate` now takes expanded years, so `skyfix almanac --date=-0584-05-22` parses too (with `=`: a leading minus after a space reads as an option; the rest of "CLI polish for far dates" above stands) |
| Almanac tables and three-day pages (Q7, almanac2) | The almanac on the labelled tier | completed | `almanac.rs` and `almanac_tables.rs` use `Sky::new()`; once the deeptime tiers land they should use the labelled-tier policy (`MERGE (deeptime)` note in `almanac_tables.rs`), since an almanac is for display, never a sight **polish2:** the pages, openings and Venus/Mars tables use the display sky (`TierPolicy::WithLabelled`); the Polaris tables keep the validated years (the a0/a1/a2 method needs the star near the pole) |
| Almanac tables and three-day pages (Q7, almanac2) | The printed almanac's own refraction and Moon radius | completed | (decided against — see notes). the tables follow this project's chain (CONVENTIONS 5), so a navigator using them gets what a reduction gets; the printed book's differ by 0.1′ in 10 of 46 published values (ACCURACY) |
| Almanac tables and three-day pages (Q7, almanac2) | The zone letters of the non-standard conditions chart | completed | (by decision — see notes). this project's zones (13 of equal air density, CONVENTIONS 13.9.1); the printed chart's own lines are not published as numbers, so a letter can differ from the book's while the exact correction for a temperature and pressure does not |
| Navigate (Q6, navigate2) | DUT1 field: blank = automatic, with the source said; site elevation in the Place editor; the stored index correction in Settings → Sights; the index-error and watch logs edited as tables, the value used shown in each sight's workings | completed | `navigate/session-panel.ts`, `dut1.ts`, `logs*.ts`, `panel/place.ts`, `shell/settings.ts` |
| Navigate (Q6, navigate2) | Compass tab: variation, compass error by azimuth and amplitude, the deviation table and its A–E curve | completed | `navigate/methods/compass.ts`, `navigate/compass/` |
| Navigate (Q6, navigate2) | Passage tab: great-circle and rhumb-line legs, times, steering points, DR now, GPX route, "Use in the running fix", forward DR; the route on the map; the measuring tool's "Add as a leg" | completed | `navigate/methods/passage.ts`, `navigate/passage/`, `map/measure.ts` (registry), `map/map-view.ts` (readout buttons) |
| Navigate (Q6, navigate2) | Sight form: the shoreline horizon's distance; "What did I shoot?"; sights only in the validated tier | completed | `navigate/sights.ts`, `shore.ts`, `starid.ts`, `tier.ts` |
| Navigate (Q6, navigate2) | Printable worksheets and the plotting sheet; the star finder on screen and printed | completed | `navigate/print/`, `navigate/starfinder/` |
| Navigate (Q6, navigate2) | Predictions with the index-error log | completed | `instrumentJson` sends it (engine/wasm-nav.ts); the mock's `predictSextant` still uses the single value |
| Navigate (Q6, navigate2) | The time-ui helpers (tier chip, `tierAt`, the calendar formatter) in place of `navigate/tier.ts`'s lookups and `dateWords` | completed | `tierAt` and `sightsOnlyText` gate the sight form, Tonight's sights, the planner and the Compass tab; the ±ΔT chip beside the sight's time, each listed sight, the DUT1 sentence and the Compass tab's time; typed dates in the display calendar (Julian before 1582-10-15, any year's width); UTC or UT after each time (`scaleLabel`); the Place panel's offset and reason under local mean time |
| Navigate (Q6, navigate2) | The ±ΔT chip beside every time in the other Navigate tabs (running fix, noon, lunar, Polaris, average) | completed | their times are in the validated tier, where the chip shows only after about 2100; the view's caution line covers them meanwhile **polish2:** every optional time field (running fix, noon, lunar, Polaris, average, plan, compass, passage) carries the chip and names the clock's scale |
| Tonight view (Q2) | The Tonight tab (eight tabs; About in the Help menu and at `#about`), the night's rule, the summary, the night's timeline, Moon, planets, deep sky, meteor showers, Milky Way, the next 14 days, tides, photography, the one-page print | completed | `web/src/next/tonight/`; `EXPLORER_GUIDE.md` "Tonight"; ui-check group `tonight`; `docs/ACCURACY.md`, "Interface: what the Tonight view works out itself" |
| Tonight view (Q2) | "Plan a photo" opening the Milky Way planner on the Selected card | completed | the best moment, then `openMilkyWayPlanner(ctx)` (`panel/photo.ts`, photo agent); on a phone the planner opens inside the bottom sheet as the sheet stands (views cannot raise the sheet) **polish2:** a view can raise the sheet now (`revealPanel`, `panel/reveal.ts`); the planner opens in the raised sheet (polish list 38) |
| Tonight view (Q2) | "Coming up" opening Events on the right tab | completed | an item sets the time, selects its body and opens Events, which shows its remembered tab; Events has no tab for conjunctions, occultations, apsides or meteor showers yet, and no way to be asked for one **polish2:** each item opens `showEvents` on its own list at its moment, with the card of an eclipse, occultation, transit or shower |
| Tonight view (Q2) | The sky's darkness (Bortle class) kept between visits | completed | kept while the page is open (per explorer); a setting in `state.ts` would keep it, shared with the Sky view's magnitude limit when sky2 adds one **polish2:** a stored setting shared with the Sky view and the Events view |
| Command line (cli3) | Every expansion-programme engine on the command line | completed | 58 subcommands, 26 to 84 (`docs/CLI.md`, "The expansion programme's engines" and the chapters after it): the sun tools, the magnetic field, sailings and star identification, time scales, deep time, packs, the Moon, deep sky, the planets, tides, the lunar limb and the almanac's tables. Each calls the WASM adapter's native layer, so `--format json` is the export's document; `crates/skyfix-cli/tests/parity.rs` holds each to its library call and most to their export, to 1e-9 |
| Command line (cli3) | "CLI `skyfix variation` / `skyfix compass-error`" (geomag rows above) | completed | with `magnetic-grid` |
| Command line (cli3) | "Command line for the sailings exports" | completed | `sailing`, `dr-advance`, `route-positions`, `star-id`, `star-finder`; the shore horizon (`--shore NM`) and the index-error log (`--ic-log UTC,ARCMIN`) on `predict`, `plan-sights` and `star-id`; `reduce` names the value a log gave |
| Command line (cli3) | "CLI access to deep sky" | completed | `dso-catalog`, `dso-list`, `dso`, `showers`, `milky-way`, `search`, `tonight`, `extinction` |
| Command line (cli3) | "No `skyfix eclipse --limb` yet" | completed | with `--pack`; the eye-safety line quotes the real limb's totality when the pack is loaded (the mean limb's can be seconds longer) |
| Command line (cli3) | "The CLI's help text" (1990-2060 in cli.rs) | completed | `almanac --date` and `seasons --year` name the coverage `explorer_coverage` reports |
| Command line (cli3) | "CLI polish for far dates" | completed | Delta-T with its standard uncertainty in the eclipse text, `delta_t_sigma_s` in the GeoJSON, every date flag takes a leading minus without `=` (`--date -0584-05-28`), the almanac's `--date` follows the CLI's calendar rule, table headings say `UT` over UT times |
| Command line (cli3) | "The daily pages' first note" | completed | `pages::NOTES[0]` now says the pages' UT is UT1, entered with UTC + DUT1 (CONVENTIONS 15.2), and `crates/skyfix-cli/tests/almanac.rs` pins it. Still quoting the old sentence: the mock almanac's note (`web/src/next/engine/mock/almanac.ts`) and the `almanac_opening` example in `docs/EXPLORER_API.md` (not the cli3 agent's to rewrite) **polish2:** the mock engine's note and EXPLORER_API's `almanac_opening` example quote the new sentence (polish list 54) |
| Command line (cli3) | `sky` and `events` on the site's DUT1 | completed | They used DUT1 = 0; they now take the IERS history (or `--dut1`, the site's field) as their exports do. No printed digit of their goldens moved |
| Command line (cli3) | Deep time on the command line (a date's tier, the tiered coverage) | completed | `tier-at` and `explorer-coverage` (`docs/CLI.md`, "Deep time"). As on the site, `sky`, `events`, `phases`, `seasons` and the sun tools answer the labelled tier, with a note under the table giving Delta-T's standard uncertainty; everything else keeps the validated tier or its engine's narrower span. The `eclipses` refusal no longer calls 1990-2060 "the Moon's" coverage |
| Events view (Q4, events2) | Moon: perigee and apogee, supermoons and micromoons, the year's largest and smallest full Moon | completed | `web/src/next/events/apsides.ts`, `moon-model.ts`; `EXPLORER_GUIDE.md` "Events" |
| Events view (Q4, events2) | Occultations seen from here (times, limb, cusp, the Moon's height, the sky, grazes, near misses, the Moon drawn with the contacts), with the Moon below the horizon, and seen elsewhere on Earth | completed | `events/occultations.ts`; the "elsewhere" rule is CONVENTIONS 15.8 and is checked against the engine's own local search (`docs/ACCURACY.md`, "Events view") |
| Events view (Q4, events2) | Planets: close approaches with the best view from the place, retrograde loops with a timeline, transits with local circumstances and the path drawn, Jupiter's moons night by night | completed | `events/{conjunctions,retrograde,transits,jupiter}.ts`, `planet-model.ts` |
| Events view (Q4, events2) | Meteor showers: the year as a calendar, ZHR, the Moon at each peak, the rate here on the peak night, tonight's rates | completed | `events/showers.ts`, `sky-model.ts`; rates labelled as the deepsky model's estimates |
| Events view (Q4, events2) | The Earth's perihelion and aphelion beside the seasons | completed | `events/lists.ts` (Seasons tab) |
| Events view (Q4, events2) | Calendar files (RFC 5545) for every list and every event, Web Share where the device takes files, CSV tables | completed | `web/src/next/export/ics.ts` (shared), `events/export-ui.ts`, `items.ts`; CONVENTIONS 15.8 |
| Events view (Q4, events2) | Deep time in the Events view: the coverage in words, the data pack's Get button under a list cut short, the ±ΔT chip and the clock's scale (UTC or UT) beside times, the lists holding still during fast playback | completed | `events/deeptime.ts` over `web/src/next/time/`; the `events/` entries in the time-ui table above ("1990 to 2060", literal "UTC") are done in this package |
| Events view (Q4, events2) | Long searches in pieces between frames, waiting while a pointer is pressed or the time moves | completed | `events/search.ts`, `shared.ts`; a Web Worker with its own WebAssembly instance would take the pieces off the main thread entirely (the engine and its loaded packs would have to be mirrored there): not done |
| Events view (Q4, events2) | `web/scripts/ui-check.mjs` header | completed | the union merge of charts2 and time-ui left a duplicated line ("than on a real screen);") and two `ONLY=` usage lines in the header comment **polish2:** the header lists the fifteen blocks in run order with their ONLY names; the blocks run in that order |
| Events view (Q4, events2) | The lunar limb in the eclipse card: corrected contacts, "Limb-corrected" or "Mean limb", the pack offered once, approximate Baily's beads | completed | `events/eclipses.ts` (`limbLine`, `fillLimbPack`), `model.ts` (`withLimb`, `correctionWords`, `beadWords`); CONVENTIONS 15.8. The list rows and the list's calendar files keep the mean limb (60-75 ms an eclipse in WebAssembly), so a row can say 3 min 51 s where the card says 3 min 48 s; the card says by how much and why **polish2:** while the pack is on the device, a central eclipse's row says "(mean limb)" after its duration, with a tip saying the card has the corrected one (polish list 42) |
| Events view (Q4, events2) | Opening Events on a list at an event from another view | completed | (the Events side). `showEvents(store, target, { jd, body, id })` and the channel `eventsRequests(store)` in `events/link.ts` (CONVENTIONS 15.8; `events-link.test.ts`, and the ids checked against the lists on the real engine). Tonight's "Coming up" still opens Events with the time and body only: its click can become `showEvents(store, eventsTargetFor(i.kind) ?? 'eclipses', { jd: i.jd, body: i.body, id: i.ref })`, with `ref` added to its eclipse, occultation, transit and shower items from `eventIds` (tonight agent's files) |
| Deep time engine (deeptime) | Coverage tiers in the core: validated 1550–2650, labelled 2000 BC–AD 3000 | completed | `skyfix_ephemeris::tiers`, `explorer_coverage` tiers, `tier_at` (`crates/skyfix-wasm/src/coverage.rs`); both tiers in the core module, so the planned `deep-time` pack was not built (EXPLORER_API "coverage tiers as built"); the historical table in `docs/ACCURACY.md` section 31 |
| Deep time engine (deeptime) | Series: VSOP87A with fitted corrections, ELP/MPP02 with refitted secular terms, long-term precession, full nutation arguments | completed | one compact file, `series.bin` (127 958 bytes); validated tier Sun 0.01″, Moon 0.36″, planets 0.04″ (Mercury) to 0.91″ (Uranus) against DE440 |
| Deep time engine (deeptime) | Stars: radial velocities; Rigil Kentaurus on alpha Cen A's orbit | completed | the Almanac's Rigil Kentaurus is A; USNO extrapolates A in a straight line (5.8″ from the orbit in 2026) |
| Deep time engine (deeptime) | The mock engine's `deep-time` pack entry | completed | `web/src/next/engine/mock/packs.ts` (packs agent) still lists a `deep-time` pack; there is none **polish2:** removed; the mock's packs are the registry's (a test compares them with `PRODUCERS` in `crates/skyfix-wasm/src/packs.rs`), and the pack prompt names only packs whose registry entry provides positions (polish list 50) |
| Verification (verify2, then docs3) | V24, V25: ELP/MPP02's and the meteor-shower table's licence basis | completed | Decided by the planner (`docs/EXPANSION_PLAN.md` §3): both stay, each with a documentation-only acknowledgement and its residual risk recorded (`docs/THIRD_PARTY.md`, "ELP/MPP02 and the meteor-shower table: kept, with the residual risk recorded"). |
| Verification (verify2, then docs3) | V26: SIMBAD's conditions and five missing hashes (tides pack, ORB6, Schureman's SP-98, pck00011) | completed | `docs/THIRD_PARTY.md`: SIMBAD's conditions stated for both uses (radial velocities, deep-sky sizes); the tides pack's own SHA-256 computed from the shipped file and recorded; ORB6, Schureman's SP-98 and pck00011.tpc honestly marked "hash not recorded" (none of the three source files was kept after reading), rather than left silently blank. |
| Verification (verify2, then docs3) | V27: CC BY 4.0 licence URIs, the at-a-glance table, stale VSOP87D/ELP/Hipparcos/MPC text | completed | `docs/THIRD_PARTY.md`: the CC BY 4.0 licence URI added at every canonical mention (IGRF-14, the SMH2016 ΔT tables, Gaia DR3, the SMH2016 paper); six rows added to the at-a-glance table (deep-sky objects, the Milky Way, meteor showers, WGSN names, Sgr A*, the GDA worked example); the stale Eclipses-section VSOP87D/ELP2000-82B claim, the Hipparcos processing note's "no radial velocity" claim, and the sky2 section's pre-verify2 MPC-credit paragraph all corrected. |
| Moonshape (P1) | DUT1 (UT1 − UTC) as an input | partial | The session's `clock.dut1_s`, the CLI's `--dut1` on every command that reduces, predicts or plans, the WASM methods, `reduce`, `solve`, the misfit grid and the navsky exports (an observer document's `dut1_s`), and the Navigate field "UT1 − UTC from the time signal". The remaining part belongs to the timescales work: the automatic value is still 0 s (`skyfix_core::time::dut1_s`, the single lookup it replaces with the IERS history and the model of CONVENTIONS 15.2) |
| The Moon in detail (P8) | Occultations of fainter stars and the Pleiades beyond Alcyone | partial | (available, not default). `max_magnitude` up to 6.5 (about 0.5 s a year natively); the default is 3.5 |
| Deep sky (deepsky) | Core-module size | partial | The package adds 164 734 bytes raw (73 118 gzipped) against an 80 KB budget; the module stays inside its 2.5 MB / 1 MB limits. About 32 KB is data, the rest code (search, showers, tonight, the night machinery, serialisation) |
| Charts (Q5) | Print from the browser's own menu (Ctrl+P) | partial | Save → Print prints one chart; the browser's own print prints the whole app |
| Deep time in the interface (Q1) | Ctrl+Page Up / Page Down in tabbed browsers | partial | (known). Chrome and Firefox keep these keys for switching tabs; the calendar's ±100 and ±1000 buttons (and the grid's Ctrl+PgUp/PgDn, which the grid receives) do the same |
| Selected card's tools (Q8, photo) | Alignment dates across a daylight-saving change | partial | (known limit). `alignment_days` lays a year on one fixed UTC offset (the one at the time shown); the list writes each day in the real zone, so only the grouping into runs of an event within an hour of local midnight could differ |
| Sky view astronomy layers (Q3, sky2) | Saturn's shadows, Jupiter's markings | partial | (known limit). the close-ups leave out the globe's shadow on the rings and the rings' on the globe, and draw Jupiter's belts where they usually are (the Great Red Spot is not tracked, ACCURACY 21) |
| Sky view astronomy layers (Q3, sky2) | The Moon close-up's maria | partial | (known limit). ellipses of each mare's size from the gazetteer, not outlines; a public-domain albedo map would need a source and a size decision |
| Sky view astronomy layers (Q3, sky2) | The Milky Way and refraction | partial | (known limit). the raster is not refracted (0.6° at most, on the horizon, under one texel) |
| Events view (Q4, events2) | Sharing calendar files | partial | (known). browsers' Web Share takes only some file types; where `canShare` refuses a calendar file, only the download is offered (phones usually open a downloaded `.ics` in the calendar) |
| Deep time engine (deeptime) | Other engines on the labelled tier | partial | The almanac pages, eclipses (1990–2060 against NASA's canon), planet events, the Moon in detail, deep sky and sailings keep the validated tier or their own range; each opens with one line (`.with_policy(TierPolicy::WithLabelled)`) once its owner has checked its own numbers there (the eclipse canon covers 2000 BC to AD 3000) **polish2:** the almanac's three-day pages and its tables answer the labelled tier (`almanac_tables::native::sky()`; the Polaris tables keep the validated tier, since their method needs the star near the pole); the others remain |
| Integration polish (polish2) | What a date in the labelled tier shows (the polish walk at 28 May 585 BC in Philadelphia and 21 June 2999 in Tromsø, every view and tab) | partial | (known). Each view says what it cannot show in one line, in plain words (`rangeWords`), with the covered years. The engines behind these views still answer only the validated tier or their own range. The Sky view draws no stars (`skyfix-starfield`; see the deeptime row above). Tonight's deep-sky ranking, meteor showers and extinction use `Sky::new()` or the star field. The Selected card's Moon and planet detail use their providers' `::new()`. The eclipse and planet-event lists cover 1990–2060, so the eclipse of 585 BC is not listed. Engine changes, one line each (`.with_policy(TierPolicy::WithLabelled)`), once each owner has checked its numbers there |
| Verification (verify2, then docs3) | V18: the ± chip's rule, scaled to each quantity's real sensitivity to ΔT | partial | Adopted by the planner (`docs/EXPANSION_PLAN.md` §3): the Sun's rise, set, transit and twilight carry no chip, the Moon's carry the scaled value, phases/seasons/conjunctions carry the full σ, and the Moon's shown position gains a chip. CONVENTIONS 15.1-15.2 and every chip call site are the `chip2` agent's to land, in parallel with this documentation pass; the guide's own chip paragraph is left for a later merge (docs3 briefed not to touch either). |
| Verification (verify2, then docs3) | V28: contract-doc drift between EXPLORER_API.md and the TypeScript/mock engines | partial | Fixed in `docs/EXPLORER_API.md` (docs3, 2026-09-25): the `deep-time` pack and `outside_validated_tier` warning that don't exist (corrected to describe the real refusal, a plain message, and `packs_loaded: []`); the stale `MERGE (deeptime)` note in the time-scales section (already merged); `ShowerNight.limiting_mag`'s missing nullable mark; `magnetic_grid`'s unenforced height limit, named as a real gap rather than a doc-only one. Still open: `sky_search`'s example line, `NightSummary`'s field table (it is documented in prose, not as a type table), the deep-sky mock's gaps (no twilight darkness, transits, planets or a Milky Way best time), the mock accepting `alignment_days` with `at_altitude` and `options` together where Rust refuses it, stale section titles in code comments, and `WasmEngine`/`MockEngine` lacking compile-time `implements` checks for `SunToolsEngine` and `GeomagEngine` — each a small, well-scoped fix for whichever agent owns that file next. |
| Sun tools | A raised horizon for alignments | open | rise and set are on the sea-level horizon; a street, a ridge or a skyline raises it. `at_altitude` covers a single known horizon altitude today; a horizon profile (altitude by azimuth) would be the full answer |
| Sun tools | An elevation term in the clear-sky estimate | open | Haurwitz has none and underestimates at high sites (Reno, Hansen & Stein 2012). Ineichen-Perez needs a Linke-turbidity climatology whose licence would have to be checked; the Bird model (a U.S. Government work) needs aerosol and water-vapour defaults |
| Sun tools | Faster Moon year series | open | a year of the Moon's rise and set (or moonrise alignments) costs about 0.3 s natively, almost all of it the event finder's 2900 exact ELP evaluations **polish2:** in WebAssembly under Node on the two-tier series, 435–560 ms for the Moon's year of rising and setting bearings at a place (`web/scripts/core-bench.mjs`, `rise_set_azimuths`; ACCURACY section 32) |
| Moonshape (P1) | Simulated Moon sights | open | `skyfix-sim` still generates altitudes on the sphere and its reverse chain knows no Moon semidiameter or parallax (`docs/NAVIGATION_SKY.md` section 6), so a simulated Moon sight would now carry the Earth-shape term as an error of up to 0.24′. The Skyfield fixtures remain the test material for Moon sights |
| Magnetic variation and compass error (geomag) | A deviation card (swinging the compass) | open | Collect compass errors on many headings into a deviation table and curve; each `compass_error` result is one heading's deviation already |
| Magnetic variation and compass error (geomag) | WMMHR2025 (degree 133, crustal field) | open | NCEI recommends it where systems can take the coefficients; about 18 000 of them (a pack, not the core). WMM2025 meets the navigation specification, and its uncertainty is stated beside every value |
| Magnetic variation and compass error (geomag) | Grid variation for polar navigation (GV) | open | Declination relative to a polar-stereographic grid north; only meaningful with a polar chart grid, which the map does not draw |
| Magnetic variation and compass error (geomag) | Magnetic variation before 1900 | open | (not planned — see notes). No standard model reaches before 1900 with a stated uncertainty (historical field models such as gufm1 exist, with their own licences and far larger errors); the programme's rule is that variation is not shown for deep time |
| Time scales (timescales) | UT1 − UTC for 1962-1972 | open | `finals2000A.all` starts in 1973; the splines stand there (σ 0.11 s, 1972 DUT1 `assumed`). IERS EOP 20 C04 (from 1962) would fill it, and needs the owner's approval to download |
| Time scales (timescales) | Regenerate the Moon, planet, topocentric and almanac-page fixtures on the CONVENTIONS 15.2 scale | open | Generated with TT = UTC + 69.184 s after 2035; their tests evaluate the fixtures' own TT and UT1 through `time::legacy_fixture_instant` and leave out the four almanac pages after 2035. Regenerate with `tools/timescales/skyfield_timescale.py`, then drop the shim (deeptime agent's generators) |
| Time scales (timescales) | Almanac page exactly at UT1 hours | open | (status was: considered). The page keeps DUT1 = 0 so each row's instant is its UT1, as printed; TT is then off by DUT1 (≤ 0.5″ of the Moon). Exact would need `pages.rs` to evaluate at UTC = hour − DUT1 |
| Time scales (timescales) | CLI polish for far dates | open | The eclipse text prints ΔT without its σ; `--format geojson` lacks `delta_t_sigma_s`; `skyfix almanac --date` takes four-digit years only (its parser is `pages::UtDate`); table headers still say "UTC" where rows now say "UT" (cli3) |
| Sailings (wave 1) | Command line for the sailings exports | open | cli3 (wave 2) |
| Deep sky (deepsky) | Double stars tonight (a curated 50-100 pairs from the USNO Washington Double Star and Sixth Orbit catalogues) | open | The brief's optional bonus. U.S. Government works; position angle and separation from ORB6 orbits |
| Deep sky (deepsky) | `meteor_showers(year, observer)` speed | open | 0.2 s natively (one night per shower, 32 nights); computing a shower's night only when the view asks for it would make the year view instant **polish2:** 150–190 ms in WebAssembly under Node for a year at a place (`web/scripts/core-bench.mjs`; ACCURACY section 32) |
| Deep sky (deepsky) | Milky Way outline: windows in the dust | open | The 100 µm dust screen darkens the whole plane, so the Sagittarius Star Cloud (M24) and similar windows come out darker than the eye sees them; a visual-band dust model or hand-set windows would fix it |
| Deep sky (deepsky) | Visibility from surface brightness | open | The instrument guide uses integrated magnitude with a size term; a surface-brightness model (with the sky's brightness) would rank faint large galaxies and nebulae better |
| Deep sky (deepsky) | CLI access to deep sky | open | No `skyfix` subcommand yet (`tonight`, `showers`, `dso`); the engine calls are ready |
| Tides | Tides outside NOAA's list | open | Other agencies' constants are licensed (UKHO, SHOM, CHS, BoM: not usable) or mixed-provenance CC BY (TICON-4); only a per-agency open source (Rijkswaterstaat CC0, a few CC BY) could add stations, each needing its own licence check (data audit, section 6) |
| Tides | Tidal currents | open | NOAA publishes current predictions (a separate harmonic product) the same way; not in this programme |
| Tides | Anchorage's last centimetre | open | 0.7 cm rms from NOAA in the diurnal band near σ1/2Q1 at the one station with NOAA's 120-constituent set; no constituent convention tried removes it (`tools/tides/README.md`) |
| Planet detail (planetdetail) | The Great Red Spot's longitude | open | , by decision. It drifts in System II by tens of degrees a year, irregularly, so no compiled value stays right; a value the person types in (from ALPO's or the BAA's current reports) would place it |
| Planet detail (planetdetail) | Galilean phenomena to seconds | open | E5 puts them 23 s (Io) to 97 s (Ganymede) from JPL; a steady per-moon along-track correction fitted to jup365, or JPL's own satellite series in a pack, would bring them under 10 s |
| Planet detail (planetdetail) | Transit contacts as seen (black drop, irradiation) | open | Contacts are geometric (the discs' tangencies); what an observer times differs by several seconds, and no published model is simple enough to be worth it |
| Planet detail (planetdetail) | Perturbed orbits for supplied elements | open | Two-body only; a numerical integration with the planets would keep near-Earth objects and Jupiter-passing comets right for months after their epoch |
| Charts (Q5) | Tier chips on chart tables and captions | open | (waiting). `// time-ui:` comments mark the places (the chart shell's tables, the tides table); merged when time-ui's helpers land |
| Charts (Q5) | Sun path hour lines (the figure-8 of each clock hour through the year) | open | the classic architect's diagram; needs the analemma at each hour (24 calls of about 10 ms natively), best computed once per place and year |
| Charts (Q5) | Tides map layer (stations on the map, the nearest highlighted) | open | EXPANSION_PLAN names a map layer; this package only marks the chosen station ("Show on the map") |
| Charts (Q5) | Moon's rise and set bearings through the year | open | `rise_set_azimuths` with `body: "Moon"` is ready (0.3 s natively a year); a panel beside the Moon through the year |
| Deep time in the interface (Q1) | Phone bottom sheet and the time bar's height | open | `panel/sheet.ts` measures the time bar only on window resize; the time bar now keeps one height on phones for every date, but a ResizeObserver there would make it robust |
| Selected card's tools (Q8, photo) | Alignments for the planets and a horizon profile | open | the engine takes any body; the finder offers the Sun and the Moon. A skyline's height per bearing (the real horizon) would replace the single "At a height" |
| The lunar limb (P12) | Third contact against NASA SVS | open | question. Second contact agrees within 1.2 s, third is 1.4 s early on average in both eclipses and in the independent implementation: SVS's central phases are 1.0-1.6 s longer than the geometric ones for the total and the annular eclipse alike, which no change in the Moon's or the Sun's size can produce. SVS's definition of "100 % coverage" and its Delta-T are not published; an SVS or NASA source that states them would settle it |
| The lunar limb (P12) | Bead-level detail | open | 1.9 km terrain resolves the main valleys only. Per-eclipse profiles from LDEM_64 (474 m) or LDEM_128, precomputed for the band of libration one eclipse spans (a few tens of kB each, data audit section 9), would give real beads |
| The lunar limb (P12) | Limb-corrected path limits on the map | open | The limits of totality move by 1-3 km with the limb; the same outline solved for the grazing site along the path's normals would draw them (`eclipse_path` is the mean limb's) |
| The lunar limb (P12) | Occultation grazes with the real limb | open | `lunar_limb_profile` gives the outline at any instant; star occultation contacts (P8, mean limb) and graze predictions could use it |
| The lunar limb (P12) | A smaller pack | open | 1.66 MB gzipped; 10 m quanta would save about 0.4 MB gzipped (at most 5 m, 0.003", of rounding), a ±10° ring about a sixth |
| The lunar limb (P12) | Command line | open | No `skyfix eclipse --limb` yet (cli3) |
| Sky view astronomy layers (Q3, sky2) | Comets and asteroids kept across visits | open | kept for the page session only, like the Sky view's own settings; keeping them would add a stored key (state.ts privacy rules: only settings and layers are stored today), a decision for the owner |
| Sky view astronomy layers (Q3, sky2) | Galaxies at their position angles | open | the deep-sky catalogue has no position angles; the ellipses are drawn level. Adding a PA column (from the same Wikidata/SIMBAD cross-check) would orient them |
| Sky view astronomy layers (Q3, sky2) | The Moon's light on the Sky view's star limit | open | the cards' estimates include it (the engine's Krisciunas & Schaefer model at each object); the chart's limit is the sky's zenith limit alone. A per-direction limit would need the engine's moonlight model per texel or per star |
| Sky view astronomy layers (Q3, sky2) | Tonight's sights rings | open | (design note). the ring marks the next twilight's bodies where they are at the time shown, which can be hours before the twilight |
| Sky view astronomy layers (Q3, sky2) | The 10 ms draw budget on a quiet machine with a GPU | open | measured on the shared machine only (ACCURACY 26): the layers add 0.5–1.5 ms to a frame; headless Chrome's median draw with every layer is 8.7–9.6 ms, its 95th percentile 17.5–23 ms (the Milky Way's raster frames). A browser with a GPU on a quiet machine (`dev-sky.html?bench=600`, the pane visible) would settle the tail |
| Almanac tables and three-day pages (Q7, almanac2) | Star charts beside the 57-star list | open | (optional in the brief). a small chart of each star among the constellation figures, from `skyfix-starfield` |
| Almanac tables and three-day pages (Q7, almanac2) | A faster opening | open | an opening is three full daily pages (0.2 s native, several times that in WebAssembly on a loaded machine), computed once the time settles. Its outer two dates' twilight tables are never shown and their second moonrise column repeats the next date's; skipping them in `pages.rs` would save about a third |
| Navigate (Q6, navigate2) | Composite sailing (a limiting latitude) in the Passage tab | open | the engine has it (`limiting_latitude_deg`); the tab offers great circles and rhumb lines only |
| Navigate (Q6, navigate2) | The deviation table applied automatically (the Compass tab's and "What did I shoot?"'s compass bearings at the ship's heading) | open | today the deviation is typed; the table's interpolated value could fill it |
| Navigate (Q6, navigate2) | A plotting sheet for the running fix (lines of position advanced to one time) | open | the Fix tab's sheet draws each line at its own time and says so |
| Navigate (Q6, navigate2) | A printed passage plan (legs, courses, times, steering points) | open | the table is on screen and in the GPX |
| Navigate (Q6, navigate2) | Importing a GPX route into the Passage tab | open | export only |
| Tonight view (Q2) | Tides beyond the night | open | the card lists the high and low water from an hour before sunset to an hour after sunrise; "next high and low" after the explorer's time is the Place section's tides line (photo) |
| Command line (cli3) | `--zone lmt` on the command line | open | the sun tools default to local mean time, as their exports do; `events`, `phases` and `seasons` take UTC, a fixed offset or a nautical zone. CONVENTIONS 15.3 shows local mean time before 1850 for a zone that follows the place; the CLI's zones are always chosen, so nothing is wrong, but an `lmt` choice (to the second, which `ResolvedZone` cannot yet hold) would match the site's historical pages |
| Command line (cli3) | `ARCHITECTURE.md` on the CLI | open | (docs). it says the CLI calls the engine crates directly; the expansion commands call the WASM adapter's native layer (`skyfix-wasm` is now a dependency of `skyfix-cli`) |
| Command line (cli3) | Request documents from a file | open | the commands build the export's document from flags; a `--request FILE` passing a saved document through unchanged would reproduce any call the site makes, including options the flags do not reach |
| Events view (Q4, events2) | Eclipse ids for dates before year 0 | open | (engine-level gap — see notes). `skyfix_almanac::eclipses::date_of` takes the first 10 characters of `format_utc`, so an eclipse in an expanded year gets a cut id (`-0584-05-2-solar`) that `by_id` refuses: its local circumstances and path fail. The interface already reads expanded-year ids (`eclipseIdDate`) |
| Events view (Q4, events2) | Transit and occultation maps (where on Earth an occultation or a grazing transit is seen) | open | the Events view says roughly which part of the Earth sees an occultation elsewhere; a map layer would need the shadow's track (a search of the local engine over a grid, or the Besselian treatment of occultations) |
| Events view (Q4, events2) | A meteor shower's rate at the place for any night, not only the peak night | open | `meteor_showers` gives the peak night; `tonight` gives the night of the time shown; a per-night rate curve for a shower would need a call per night (about 40 ms natively) |
| Events view (Q4, events2) | The limb drawn on the eclipse card (the profile with the Sun's disc at second and third contact) | open | `block.profile` is at maximum eclipse only; a drawing near the contacts needs `lunar_limb_profile` at their instants and an exaggerated height scale, which wants a design pass |
| Deep time engine (deeptime) | Star field in the labelled tier | open | `skyfix-starfield` answers the validated tier (it was 1800–2200); its frame already works at any date and its catalogue's linear proper motions stay within about a minute of arc at 2000 BC, so the Sky view could draw it there too (display only) |
| Deep time engine (deeptime) | Rigil Kentaurus's light centre and barycentre | open | A sextant sees the A+B light centre (about 2″ from A in 2026); the barycentre's proper motion is uncertain by 15–30 mas a year (about 10″ at the validated tier's edges). a modern determination of the system's barycentric motion from both components' astrometry would tighten the second |
| Deep time engine (deeptime) | The almanac page for 2650-01-22 | open | The validated tier ends at 00:00 of that date, so its page is accepted with every hour clamped to one instant; `pages.rs` could refuse a date whose 24 hours are not all covered |
| Integration polish (polish2) | The pack prompt on a phone's short stage | open | (design, packs). On a phone the stage between the time bar and the sheet is about 280 px tall. The pack prompt (bottom centre, `packs/packs.css`) and the offline-ready card stack over its lower half. On Charts → Tides they cover the card's Earlier/Later, Day/Week and Save until the prompt is answered (the offline card goes by itself after a few seconds). The prompt could go into the sheet on phones, or fold to one line like the notices do |
| Integration polish (polish2) | The Sky view's draw budget (10 ms) | open | The median draw is 9.1–12.4 ms at load averages of 5–12.5 on the shared machine, the same with either `opt-level` (ACCURACY section 32). Not measured on a quiet machine. Leaving out the Milky Way and grid layers at low zoom is the first remedy if it needs margin (not done) |
| Integration polish (polish2) | `sky/view.ts` split by layer | open | About 2 450 lines. A split with no change in behaviour was left out of the polish pass for lack of time |
| Integration polish (polish2) | `gen_planetdetail.py` `--window` and `--kernel` | open | The other generators take them (`tools/reference/common.py`). This one's fixtures come from several sources at fixed epochs (Horizons, `jup365.bsp`, NASA's catalogues), so the options need a design, not a pass-through |
| Integration polish (polish2) | Further module-size levers | open | (status was: noted). In cargo's output, serde's code is 738 KB of the 2.38 MB of code, and the sorts' repeated copies are about 115 KB (ACCURACY section 32). One serialiser would change the wire contract; a shared sort would change how the engines order their events. The module is 450 KB raw and 98 KB gzipped inside its budget |
| Integration polish (polish2) | Behaviour for the owner to confirm | open | (owner). The rise and set cards go blank while time plays faster than a month a second. The phone's time bar is two lines at a fixed height |
| Integration polish (polish2) | The Learn view while the time bar is dragged | open | Learn does not use the time, yet a drag costs 14–19 ms of script a frame there (100–117 ms frames in headless Chrome), against 1.4 ms on About. Main at 1bff16e measures the same, so the cost predates this pass. Not investigated (ui-check's scrub block reports it) |
| Verification (verify2, then docs3) | V13: a CI job that runs the web typecheck and tests against the built core | completed | Added by the planner in commit ec00e88 (after docs3 branched): `.github/workflows/ci.yml` job "Web (typecheck and tests against the built core)" installs wasm-pack, builds the core, runs `npm run typecheck` and `vitest --maxWorkers=2`; green on its first run. |
