[package]
name = "skyfix-almanac"
description = "Events (rise, set, transit, twilight, phases, seasons), sky state, almanac pages and eclipses for SkyFix Lab. No network, no filesystem."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
skyfix-core = { path = "../skyfix-core" }
skyfix-ephemeris = { path = "../skyfix-ephemeris" }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
approx = { workspace = true }
