/**
 * Explorer entry point (`/next/`). OWNER: shell agents (docs/EXPLORER_PLAN.md §4).
 * Placeholder until the shell lands: says what this page will be, computes nothing.
 */
const app = document.getElementById('app');
if (app) {
  app.textContent =
    'The new SkyFix Lab explorer is being built here. The current workbench is one level up.';
}
