/**
 * The Learn view's entry for the explorer shell's registry, which finds a view at
 * `src/next/<folder>/view.ts` by its default export (shell/registry.ts). OWNER: learn agent.
 * The component itself lives in index.ts.
 */

export { learn as default } from './index.js';
